# Problems skipped by CI verify.sh because they are too slow (>30s),
# crash, or fail to compile. Each line is a problem number.
# Format: NNN reason
#
# Timeout (>120s on local scan):
#
# Runtime crash / abort:
#
# Compilation error:
#
# Slow (>30s, risk of CI timeout):
