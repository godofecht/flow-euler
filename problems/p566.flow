# Project Euler 566
# Cake Icing Puzzle — signed-permutation atomization + CRT on flip cycles.

extern {
    function pe566_answer() -> i64
}

function main() -> i32 {
    printf("%lld\n", pe566_answer())
    return 0
}
