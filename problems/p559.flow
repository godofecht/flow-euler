# Project Euler 559
# Permuted Matrices — factorial powers + series inversion.

extern {
    function pe559_answer() -> i64
}

function main() -> i32 {
    printf("%lld\n", pe559_answer())
    return 0
}
