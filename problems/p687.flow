# Project Euler 687
# Shuffling cards: P(#perfect ranks is prime) to 10 decimals.

extern {
    function pe687_answer() -> f64
}

function main() -> i32 {
    printf("%.10f\n", pe687_answer())
    return 0
}
