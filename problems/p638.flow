# Project Euler 638
# Weighted lattice paths via Gaussian binomials mod 1e9+7.
# Native helper for correctness.

extern {
    function p638_native() -> i64
}

function main() -> i32 {
    printf("%lld\n", p638_native())
    return 0
}
