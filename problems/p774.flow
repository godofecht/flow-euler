# Project Euler 774
# Divide and Rule.  Real solver in problems/native/p774.c (linked by run.sh).

extern {
    function p774_native() -> i64
}

function main() -> i32 {
    printf("%lld\n", p774_native())
    return 0
}
