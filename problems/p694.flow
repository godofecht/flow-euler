# Project Euler 694
# Cube-full divisors: S(10^18) via DFS over p^3 | n cube-fulls.

extern {
    function pe694_answer() -> i64
}

function main() -> i32 {
    printf("%lld\n", pe694_answer())
    return 0
}
