# Project Euler 859
# Cookie Game: count unordered pile partitions of 300 where Even wins.
# Real solver in problems/native/p859.c (linked by scripts/run.sh).

extern {
    function p859_native() -> i64
}

function main() -> i32 {
    printf("%lld\n", p859_native())
    return 0
}
