# Project Euler 650
# Divisors of binomial product: track sigma(B_n) mod 1e9+7 up to n=20000.

extern {
    function pe650_answer() -> i64
}

function main() -> i32 {
    printf("%lld\n", pe650_answer())
    return 0
}
