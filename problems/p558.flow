# Project Euler 558
# Real solver in problems/native/p558.{c,cpp} (linked by scripts/run.sh).

extern {
    function pe558_answer() -> i64
}

function main() -> i32 {
    printf("%lld\n", pe558_answer())
    return 0
}
